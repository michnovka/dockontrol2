<div class="card border-0 shadow-none">
    {{ form_start(form) }}
    <div class="card-body">
        <div class="row">

            <div class="col-md-12">
                {{ form_row(form.adminBuildings) }}
            </div>

            <div class="col-md-2 text-start">
                <button class="btn btn-primary w-100">Save Changes</button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>

</div>
