<div class="card border-0 shadow-none container">
    {{ form_start(customSortingGroupForm) }}
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                {{ form_row(customSortingGroupForm.name) }}
            </div>

            <div class="col-md-6">
                {{ form_row(customSortingGroupForm.columnSize) }}
            </div>

            <div class="col-md-12">
                <button type="submit" class="btn btn-primary w-100 btn-lg">SAVE</button>
            </div>
            {{ form_end(customSortingGroupForm) }}
        </div>
    </div>
</div>

